#include <iostream>

#include <cstring>
#include <vector>

#include "solver.h"

using std::cout; using std::endl; using std::vector;

int main() {
  Solver s;
  vector<double> yVals(10);
  s.solve();
  return 0;
}
